# Project Rules

## GitHub 操作

- GitHub の操作（Issue、PR、リポジトリ情報の取得など）には **常に `gh` CLI を使用する**
- MCP の GitHub ツール（`mcp__github__*`）は認証が通らないため **使用禁止**
- 例: `gh issue view 123`, `gh pr create`, `gh pr view 123`
