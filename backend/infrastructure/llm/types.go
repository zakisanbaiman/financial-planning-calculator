package llm

import "github.com/financial-planning-calculator/backend/application/ports"

// StreamChunk はports.StreamChunkの型エイリアス（後方互換性のため）
type StreamChunk = ports.StreamChunk

// LLMClient はports.LLMClientの型エイリアス（後方互換性のため）
type LLMClient = ports.LLMClient
